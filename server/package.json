{
  "name": "chelsey-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Backend for Chelsey — holds API keys, creates Tavus conversations, proxies Claude as an OpenAI-compatible LLM.",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "reset-persona": "node -e \"import('node:fs').then(fs=>{try{fs.unlinkSync('.persona-id');console.log('persona cache cleared')}catch{console.log('nothing to clear')}})\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
